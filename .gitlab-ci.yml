stages:
    - deploy

deploy-dev:
  stage: deploy
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."
    - pwd
    - whoami
    - ls
  tags:
    - dev-srv
  environment: develop
