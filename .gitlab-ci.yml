stages:
  - deploy

deploy-dev:
  stage: deploy
  variables:
    GIT_STRATEGY: clone
  script:
    - echo "This job deploys something from the $CI_COMMIT_BRANCH branch."
    - whoami
    - pwd
    - ls
  tags:
    - dev-srv